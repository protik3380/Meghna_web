@using EFreshStore.Models.Context
@using Vereyon.Web
@model IEnumerable<EFreshStore.Models.Context.Thana>

@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_CommonMaster.cshtml";
    List<Thana> thanas = ViewBag.Thanas;
}

<div class="chart-area">
    <div class="container">
        @Html.RenderFlashMessages()
        <h2>Thana Details</h2>
        <div class="row">
            <div class="col-md-12">
                <div class="chart-item table-responsive fix">
                    <form>
                        <fieldset id="text">
                            <table class="col-md-12" id="tblCartProduct">
                                <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>District</th>
                                        <th>Edit</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model != null)
                                    {
                                        foreach (var thana in thanas)
                                        {
                                            <tr>
                                                <td>@thana.Name</td>
                                                <td>@thana.District.Name</td>
                                                <td>
                                                    @Html.ActionLink("Edit", "Edit", new {id = thana.Id}, htmlAttributes: new {@style = "color: red"})
                                                </td>
                                            </tr>
                                        }
                                    }
                                </tbody>
                            </table>
                        </fieldset>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>